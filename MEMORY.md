# 🧠 Repository Memory

> Auto-generated by [repo-memory](https://github.com/AakashPawar/repo-memory). Do not edit manually.
> Last indexed: 2026-03-02T19:47:03Z | Commit: 65eed00
> **repo-memory** | 15 files | 129 symbols | Primary: Typescript

---

## 📋 Project Overview
- **Languages**: Typescript (100%)
- **Package Manager**: npm
- **Build Tool**: tsup
- **Test Framework**: Vitest
- **Entry Points**: `dist/cli.js`
- **Build**: `npm run build`
- **Test**: `npm test`
- **Dev**: `npm run dev`

---

## 🏗️ Architecture Map

```
src/ (11 files)
├── config/ (1 files)
├── generator/ (1 files)
├── indexer/ (4 files)
├── store/ (1 files)
└── utils/ (3 files)
```

---

## 🗂️ Key Files & Symbols

### `src/indexer/parser.ts` (L1-L1036)
- 📐 **`ParsedSymbol`** (L4-L33) `exported`
  - `interface ParsedSymbol`
- 📐 **`ParsedImport`** (L35-L44) `exported`
  - `interface ParsedImport`
- 📐 **`ParsedFile`** (L46-L57) `exported`
  - `interface ParsedFile`
- ⚡ **`parseFile`** (L63-L82) `exported`
  - `function parseFile(file: ScannedFile): ParsedFile`
  - Parse a scanned file and extract symbols and imports. Uses regex-based extraction (reliable across a...
- 📌 **`symbols`** (L92-L92)
- 📌 **`imports`** (L93-L93)
- 📌 **`exports`** (L94-L94)
- 📌 **`currentClass`** (L96-L96)
- 📌 **`symbols`** (L395-L395)
- 📌 **`imports`** (L396-L396)
- 📌 **`exports`** (L397-L397)
- 📌 **`currentClass`** (L399-L399)
- ⚡ **`parseGo`** (L521-L524)
  - `function parseGo(content: string, path: string, language: string): ParsedFile`
- 📌 **`symbols`** (L523-L523)
- 📌 **`imports`** (L524-L524)
- 📌 **`symbols`** (L635-L635)
- 📌 **`imports`** (L636-L636)
- 📌 **`symbols`** (L750-L750)
- 📌 **`imports`** (L751-L751)
- 📌 **`currentClass`** (L753-L753)
- 📌 **`symbols`** (L835-L835)
- 📌 **`imports`** (L836-L836)
- ⚡ **`findBlockEnd`** (L895-L918)
  - `function findBlockEnd(lines: string[], startIndex: number): number`
  - Find the end of a brace-delimited block starting from the given line. /
- ⚡ **`extractJSDoc`** (L940-L963)
  - `function extractJSDoc(lines: string[], lineIndex: number): string | null`
  - Extract JSDoc comment above a line. /
- 📌 **`docs`** (L941-L941)
- 📌 **`docLines`** (L979-L979)
- ⚡ **`extractGoDoc`** (L996-L1008)
  - `function extractGoDoc(lines: string[], lineIndex: number): string | null`
  - Extract Go doc comment above a line (// comments). /
- 📌 **`docs`** (L997-L997)
- ⚡ **`extractRustDoc`** (L1013-L1025)
  - `function extractRustDoc(lines: string[], lineIndex: number): string | null`
  - Extract Rust doc comment above a line (/// comments). /
- 📌 **`docs`** (L1014-L1014)
- ⚡ **`buildSignature`** (L1030-L1035)
  - `function buildSignature(line: string): string`
  - Build a clean function signature. /

### `src/store/database.ts` (L1-L462)
- 📐 **`StoredFile`** (L6-L14) `exported`
  - `interface StoredFile`
- 📐 **`DbFile`** (L16-L24)
  - `interface DbFile`
- 📐 **`DbSymbol`** (L26-L39)
  - `interface DbSymbol`
- 🏛️ **`MemoryDatabase`** (L44-L461) `exported`
  - `class MemoryDatabase`
  - SQLite-based storage for repo-memory index. /
- 🔧 **`MemoryDatabase.constructor`** (L47-L55)
  - `constructor(dbPath: string)`
- 🔧 **`MemoryDatabase.initSchema`** (L57-L115)
  - `private initSchema()`
- 🔧 **`MemoryDatabase.UNIQUE`** (L89-L112)
  - `UNIQUE(source_file_id, target_file_id, import_specifier)`
- 🔧 **`MemoryDatabase.VALUES`** (L129-L142)
  - `VALUES (?, ?, ?, ?, CURRENT_TIMESTAMP, ?)`
- 🔧 **`MemoryDatabase.insertSymbol`** (L149-L186)
  - `insertSymbol(fileId: number, symbol: ParsedSymbol)`
  - Insert a symbol for a file. /
- 🔧 **`MemoryDatabase.VALUES`** (L152-L183)
  - `VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`
- 🔧 **`MemoryDatabase.VALUES`** (L173-L264)
  - `VALUES (?, ?, ?, ?, ?)`
- 🔧 **`MemoryDatabase.clearFileSymbols`** (L191-L193)
  - `clearFileSymbols(fileId: number)`
  - Clear all symbols for a given file (before re-indexing). /
- 🔧 **`MemoryDatabase.clearFileDependencies`** (L198-L202)
  - `clearFileDependencies(fileId: number)`
  - Clear all dependencies for a source file. /
- 🔧 **`MemoryDatabase.VALUES`** (L217-L240)
  - `VALUES (?, ?, ?, ?)`
- 🔧 **`MemoryDatabase.getFile`** (L226-L232)
  - `getFile(path: string): StoredFile | null`
  - Get a file by path. /
- 🔧 **`MemoryDatabase.getFileHash`** (L237-L242)
  - `getFileHash(path: string): string | null`
  - Get file hash for change detection. /
- 🔧 **`MemoryDatabase.getAllFileHashes`** (L247-L252)
  - `getAllFileHashes(): Map<string, string>`
  - Get all file paths and hashes. /
- 🔧 **`MemoryDatabase.removeDeletedFiles`** (L257-L266)
  - `removeDeletedFiles(existingPaths: Set<string>)`
  - Remove files that no longer exist. /
- 🔧 **`MemoryDatabase.getFileId`** (L271-L276)
  - `getFileId(path: string): number | null`
  - Get file ID by path. /
- 🔧 **`MemoryDatabase.searchSymbols`** (L281-L310)
  - `searchSymbols(query: string, limit: number = 20)`
  - Full-text search across symbols. /
- 🔧 **`MemoryDatabase.getFileSymbols`** (L315-L319)
  - `getFileSymbols(fileId: number)`
  - Get all symbols for a file. /
- 🔧 **`MemoryDatabase.getAllFiles`** (L324-L337)
  - `getAllFiles(): StoredFile[]`
  - Get all files. /
- 🔧 **`MemoryDatabase.getAllSymbols`** (L342-L368)
  - `getAllSymbols()`
  - Get all symbols. /
- 🔧 **`MemoryDatabase.getFanInCounts`** (L373-L387)
  - `getFanInCounts(): Map<string, number>`
  - Get dependency fan-in for each file. /
- 🔧 **`MemoryDatabase.getExternalPackages`** (L392-L405)
  - `getExternalPackages(): Map<string, number>`
  - Get external packages and their usage count. /
- 🔧 **`MemoryDatabase.setMetadata`** (L410-L414)
  - `setMetadata(key: string, value: string)`
  - Set metadata. /
- 🔧 **`MemoryDatabase.getMetadata`** (L419-L424)
  - `getMetadata(key: string): string | null`
  - Get metadata. /
- 🔧 **`MemoryDatabase.getStats`** (L429-L446)
  - `getStats()`
  - Get total stats. /
- 🔧 **`MemoryDatabase.transaction`** (L451-L453)
  - `transaction<T>(fn: () => T): T`
  - Run operations in a transaction. /
- 🔧 **`MemoryDatabase.close`** (L458-L460)
  - `close()`
  - Close the database. /

### `src/generator/memory-generator.ts` (L1-L452)
- 📐 **`GeneratorContext`** (L12-L19)
  - `interface GeneratorContext`
- ⚡ **`generateMemoryFile`** (L24-L71) `exported`
  - `function generateMemoryFile(ctx: GeneratorContext): string`
  - Generate the MEMORY.md file from the indexed data. /
- 📌 **`sections`** (L25-L25)
- ⚡ **`getMemoryTokenCount`** (L76-L78) `exported`
  - `function getMemoryTokenCount(content: string): number`
  - Get estimated token count of the generated output. /
- ⚡ **`generateProjectOverview`** (L82-L119)
  - `function generateProjectOverview(ctx: GeneratorContext): string`
- 📌 **`lines`** (L86-L86)
- ⚡ **`generateArchitectureMap`** (L121-L131)
  - `function generateArchitectureMap(ctx: GeneratorContext): string`
- 📌 **`lines`** (L122-L122)
- ⚡ **`generateKeySymbols`** (L133-L219)
  - `function generateKeySymbols(ctx: GeneratorContext): string`
- 📌 **`lines`** (L134-L134)
- ⚡ **`generateDependencySection`** (L221-L266)
  - `function generateDependencySection(ctx: GeneratorContext): string`
- 📌 **`lines`** (L222-L222)
- ⚡ **`generateConventionsSection`** (L268-L289)
  - `function generateConventionsSection(ctx: GeneratorContext): string`
- 📌 **`lines`** (L270-L270)
- ⚡ **`generateCriticalPaths`** (L291-L331)
  - `function generateCriticalPaths(ctx: GeneratorContext): string`
- 📌 **`lines`** (L292-L292)
- 📐 **`TreeNode`** (L335-L341)
  - `interface TreeNode`
- ⚡ **`buildDirectoryTree`** (L343-L377)
  - `function buildDirectoryTree(files: ScannedFile[], maxDepth: number): TreeNode`
- 📌 **`root`** (L344-L344)
- ⚡ **`annotateTree`** (L379-L393)
  - `function annotateTree(node: TreeNode): number`
- ⚡ **`renderTree`** (L395-L423)
  - `function renderTree(node: TreeNode, prefix: string, isRoot: boolean): string[]`
- 📌 **`lines`** (L396-L396)
- ⚡ **`getSymbolIcon`** (L425-L439)
  - `function getSymbolIcon(kind: string): string`
- 📌 **`icons`** (L426-L426)
- ⚡ **`cleanSignature`** (L441-L447)
  - `function cleanSignature(sig: string): string`
- ⚡ **`capitalize`** (L449-L451)
  - `function capitalize(str: string): string`

### `src/utils/git.ts` (L1-L103)
- ⚡ **`isGitRepo`** (L8-L18) `exported`
  - `function isGitRepo(dir: string): boolean`
  - Check if the given directory is a git repository. /
- ⚡ **`getGitRoot`** (L23-L33) `exported`
  - `function getGitRoot(dir: string): string | null`
  - Get the root of the git repository. /
- ⚡ **`getChangedFiles`** (L38-L58) `exported`
  - `function getChangedFiles(dir: string, sinceRef?: string): string[]`
  - Get list of files changed since last index (or all tracked files if no ref given). /
- ⚡ **`getCurrentCommitHash`** (L63-L73) `exported`
  - `function getCurrentCommitHash(dir: string): string | null`
  - Get the current HEAD commit hash (short). /
- ⚡ **`getGitignorePatterns`** (L78-L87) `exported`
  - `function getGitignorePatterns(dir: string): string[]`
  - Read .gitignore patterns from a directory. /
- ⚡ **`getRemoteUrl`** (L92-L102) `exported`
  - `function getRemoteUrl(dir: string): string | null`
  - Get the remote origin URL. /

### `src/indexer/dependency-resolver.ts` (L1-L239)
- 📐 **`DependencyEdge`** (L5-L14) `exported`
  - `interface DependencyEdge`
- 📐 **`DependencyGraph`** (L16-L27) `exported`
  - `interface DependencyGraph`
- 📌 **`edges`** (L37-L37)
- ⚡ **`isExternalImport`** (L151-L167)
  - `function isExternalImport(specifier: string, language: string): boolean`
  - Check if an import is external (npm package, stdlib, etc.) /
- ⚡ **`getPackageName`** (L172-L178)
  - `function getPackageName(specifier: string): string`
  - Extract package name from import specifier. /
- ⚡ **`detectCircularDeps`** (L183-L224)
  - `function detectCircularDeps(edges: DependencyEdge[]): Array<[string, string]>`
  - Simple circular dependency detection using DFS. /
- 📌 **`circular`** (L185-L185)

---

## 🔗 Dependency Graph

### 📦 External Dependencies (by usage)
- `node:path` — used in 8 files
- `node:fs` — used in 7 files
- `chalk` — used in 2 files
- `commander` — used in 1 files
- `ora` — used in 1 files
- `better-sqlite3` — used in 1 files
- `fast-glob` — used in 1 files
- `ignore` — used in 1 files
- `node:crypto` — used in 1 files
- `node:child_process` — used in 1 files

---

## 📐 Conventions & Patterns
- **File Naming**: mixed
- **Function Naming**: camelCase

---

## 🚨 Critical Paths

Files with highest impact — changes here cascade across the codebase:

_No critical dependency hotspots detected (all files have < 3 dependents)._